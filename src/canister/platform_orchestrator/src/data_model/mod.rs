/// Stateless canister data. No heap state is persisted across upgrades.
/// All operational state is driven off-chain and passed as function parameters.
#[derive(Default)]
pub struct CanisterData {}
